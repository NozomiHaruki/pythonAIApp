import streamlit as st

from core.ui import render_sidebar

st.set_page_config(page_title="AI ライティングスタジオ", page_icon="✍️", layout="wide")

render_sidebar()

st.title("✍️ AI ライティングスタジオ")
st.caption("Gemini API を使った個人用ライティング支援ツール集")
st.divider()

st.markdown(
    """
左のサイドバーの **Pages** から使いたいツールを選んでください。
初回利用時は、サイドバー上部で Gemini API キーを入力してください（`.env` の `GEMINI_API_KEY` からも自動読込されます）。

### 利用できるツール

| ツール | 概要 |
|---|---|
| 📝 ブログ記事作成 | テーマや読者を指定してブログ記事の下書きを生成 |
| 📧 メール返信作成 | 受信メールと伝えたい要点から返信文を生成 |
| 📄 文章要約 | 長文を指定した長さ・形式で要約 |
| ✨ 校正・リライト | 誤字脱字チェックや文体変更（丁寧・簡潔・カジュアルなど） |
| 📱 SNS投稿文生成 | X / Instagram など媒体別の投稿文を複数案生成 |
| 🌐 翻訳 | 多言語への自然な翻訳 |
| 💡 タイトル・キャッチコピー生成 | 記事タイトルや広告コピーを複数案生成 |

### 使い方のヒント
- 生成結果はテキストエリアに表示されるので、そのままコピーするか `.txt` として保存できます。
- サイドバーの temperature を上げるとより創造的な、下げるとより安定した出力になります。
"""
)
